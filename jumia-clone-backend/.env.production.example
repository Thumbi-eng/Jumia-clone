# Production Environment Variables Template
# Copy this to .env.production and fill in your actual values

# Database Configuration (Neon.tech, Supabase, or Cloud SQL)
DB_HOST=your-db-host.neon.tech
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_secure_password
DB_SSLMODE=require

# Separate databases for each service
DB_NAME_USERS=jumia_users
DB_NAME_PRODUCTS=jumia_products

# JWT Authentication
JWT_SECRET=your-very-secure-jwt-secret-key-change-this

# Service Ports (for Cloud Run)
USER_SERVICE_PORT=50051
PRODUCT_SERVICE_PORT=50052
CART_SERVICE_PORT=50053
ORDER_SERVICE_PORT=50054
API_GATEWAY_PORT=8080

# Cloud Run Service URLs (filled automatically by deploy script)
USER_SERVICE_URL=https://user-service-xxx.run.app
PRODUCT_SERVICE_URL=https://product-service-xxx.run.app
CART_SERVICE_URL=https://cart-service-xxx.run.app
ORDER_SERVICE_URL=https://order-service-xxx.run.app

# GCP Configuration
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1

# Instructions:
# 1. Get free PostgreSQL from https://neon.tech or https://supabase.com
# 2. Create two databases: jumia_users and jumia_products
# 3. Copy connection details above
# 4. Generate a secure JWT secret: openssl rand -base64 32
# 5. Run: ./deploy-gcp.sh
